# Dipendenze del runtime (le stesse di pyproject.toml, per chi preferisce pip -r).
# python3.12
fastapi>=0.115
starlette>=0.41
uvicorn>=0.30
jinja2>=3.1
bcrypt>=4.1
cryptography>=42
httpx>=0.27
segno>=1.6

# sviluppo
pytest>=8.0
